add_subdirectory(frontend)
add_subdirectory(imports)
if(ANDROID)
    message("Using AIDL backend")
    add_subdirectory(backend_aidl)
else()
    message("Using simulation backend")
    add_subdirectory(backend_simulator)
endif()
